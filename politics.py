from multiprocessing import Process
import os
import time 
import threading
import math
import system 

def politics(polProblem)

    polProblem bool = false

    while polProblem == false : 
        proba = float(math.random())
        if proba >= 0,90 :
            polProblem == true
            
    
    typeProbPol = int(math.random()*2)+1
    if typeProbPol == 1 : #Embargo 
        #Fonction de market qui restreint la demande énergétique
    elif typeProbPol == 2 : #War
        #Fonction de market qui restreint la demande énergétique 
     

    









